{
  "name": "name-name-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies GitHub REST API for name-name games (Agasteer-style).",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@octokit/core": "^5.2.0",
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.0",
    "@cloudflare/workers-types": "^4.20240512.0",
    "@octokit/types": "^13.0.0",
    "typescript": "^5.4.5",
    "undici": "^6.25.0",
    "vitest": "^1.5.0",
    "wrangler": "^3.57.0"
  }
}
