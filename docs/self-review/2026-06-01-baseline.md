# セルフレビュー: 初回ベースライン診断（2026-06-01）

`/dev-doctrine name-name init` 適用時のコードベース診断。6 規律に照らした現状スナップショット。
以後の改善はこのベースラインからの差分で測る。

## 規律別の充足状況

| 規律 | 状態 | 根拠 |
|---|---|---|
| 1. docs を正本に | ✅ | architecture.md / ADR / spec が充実。本 init で guidelines / roadmap / self-review を追加 |
| 2. マスタ/ドメイン分離 | ✅ | `*Def` 命名統一（`types.ts:77-142`）+ parser `master.rs`。可変状態は GameState/RPGProject |
| 3. GameState 完全シリアライズ + 任意局面起動 | ⚠️ 部分 | `NovelGameState`（`GameState.ts`）✅、`applyState`✅(private) / **`playScript`・`startFrom` public API 未実装（#220）** |
| 4. 単一責務 / 網状依存 | ⚠️ 問題 | `NovelRenderer` 1874行・`RaycastRenderer` 1857行・`TopDownRenderer` 925行・`DialogBox` 856行の god-object 傾向。一方 `raycastProjection.ts`/`mapValidation.ts` の純粋関数切り出しは手本 |
| 5. 設計先行 | — | ロードマップで二段構えを規約化（運用で測る） |
| 6. セルフレビューで育てる | 🆕 | 本ファイルが初回。以後継続 |

## 直すべき項目（roadmap に登録済み）

1. **ADR 0002 の public API 実装**（#220 Phase 1-3: playScript / startFrom / URL クエリ）— 設計と実装のギャップ。最優先
2. **レンダラの漸進的責務分割** — 新機能追加のたびに計算部を純粋関数化する規約を回す

## 先送り（理由つき）

- レンダラの一括分割はしない。リグレッションリスクが高く、`raycastProjection.ts` 方式の漸進リファクタが安全（ガイドライン規律4）

## 所見

土台（型分離・テスト文化・状態スナップショット設計）は強い。「設計は済んでいるが public 化が遅れている」状態。
規律3 の public API を出すことが、name-name の決定論的デバッグという売りを実機で回せる形にする鍵。
