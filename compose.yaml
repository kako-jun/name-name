services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "7373:7373"
    volumes:
      - ./backend/app:/app/app
      - ./backend/projects:/app/projects
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn app.main:app --host 0.0.0.0 --port 7373 --reload
    networks:
      - name-name-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
      - ./frontend/index.html:/app/index.html
      - ./frontend/vite.config.ts:/app/vite.config.ts
    environment:
      - VITE_API_URL=http://localhost:7373
    command: npm run dev -- --host 0.0.0.0
    networks:
      - name-name-network
    depends_on:
      - backend

networks:
  name-name-network:
    driver: bridge

volumes:
  backend-projects:
